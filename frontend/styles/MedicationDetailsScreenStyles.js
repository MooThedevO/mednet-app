import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#fff',
  },
  detailText: {
    fontSize: 16,
    marginVertical: 5,
  },
});

export default styles;
